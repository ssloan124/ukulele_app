apiVersion: v1
kind: Service
metadata:
    name: "testakscluster"
    labels:
        app: "testakscluster"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
    selector:
        app: "testakscluster"